# Changelog

Historial de cambios de los proyectos 2 y 3.

## Proyecto 2

### 2026-04-29

#### Changed
- Eder-Ceballos: estructuracion proyecto 2/ carpetas (`f817e3c`).
- Eder-Ceballos: estructuracion proyecto 2 (`87b8e53`).

#### Added
- Eder-Ceballos: md5 proyecto 2 (`cf72c83`).
- Eder-Ceballos: md5 proyecto 2 (`0665e6a`).

### 2026-04-22

#### Added
- isa-ruiz: implementar Computer (`47516e1`).
- isa-ruiz: implementar CPU (`3b8577b`).
- isa-ruiz: implementar shifter (`c5ee1bb`).

### 2026-04-14

#### Changed
- isa-ruiz: actualizar README's (`9c1eb1e`).

#### Added
- isa-ruiz: CONTRIBUTORS add (`50f8fd8`).

### 2026-04-13

#### Changed
- isa-ruiz: organizar estructura de charpetas (`3b8a37c`).

## Proyecto 3

### 2026-04-28

#### Added
- isa-ruiz: agrega archivos md5 de HackAssembler (`39605d4`).

### 2026-04-22

#### Added
- isa-ruiz: documentacion proyecto 3 (`7453474`).
- Eder-Ceballos: incluimos el Hack assembler (`996be7d`).
- Eder-Ceballos: incluimos el parser (`c5d977f`).
- isa-ruiz: HackAssemblerTest (`f66336b`).
- isa-ruiz: implementar HackAssembler (`ab9d61e`).
- isa-ruiz: implementar Code.java (`777f2d5`).

#### Changed
- isa-ruiz: merge branch `main` de `https://github.com/SailBliss/organizacionComputadores` (`24a2b00`).
- isa-ruiz: ultimos ajustes (`f371611`).
- isa-ruiz: ultimos ajustes (`bab536b`).

### 2026-04-16

#### Added
- isa-ruiz: implementar SymbolTable con los simbolos Hack predefinidos (`b8525bd`).
- isa-ruiz: add pom.xml (`71236b9`).

### 2026-04-14

#### Changed
- isa-ruiz: actualizar README's (`9c1eb1e`).

#### Added
- isa-ruiz: CONTRIBUTORS add (`50f8fd8`).

### 2026-04-13

#### Changed
- isa-ruiz: organizar estructura de charpetas (`3b8a37c`).
